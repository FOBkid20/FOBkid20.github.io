{
  "name": "fobkid20-github-io",
  "private": true,
  "version": "1.0.0",
  "description": "Test suite for the static portfolio site",
  "scripts": {
    "test:unit": "node --test tests/unit/*.test.js",
    "test:integrity": "node --test tests/integrity/*.test.js",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:integrity && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "cheerio": "^1.0.0"
  }
}
